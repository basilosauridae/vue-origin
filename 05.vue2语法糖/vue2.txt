1.默认、按需、直接导入/导出
2.安装依赖包 -D作为开发阶段的依赖
3.webpack开发服务器

4.由于vue是单页面应用程序，所有组件结构基于唯一的index.html呈现，所以组件默认样式会影响到全局；解决方法：在组件style加上scoped属性，让样式只作用于当前组件

5.组件通信
  (1)data中数据允许修改，props数据不允许修改，遵循单向数据流开发规范
  (2)子传父，子组件通过 this.$emit('事件名',参数)触发事件同时传参，
    父组件给子组件注册自定义事件，并提供对应函数接收参数 @自定义事件="对应函数"